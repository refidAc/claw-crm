# Database
DATABASE_URL=postgresql://crm:crm_password@localhost:5432/crm_db

# Redis
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars
BETTER_AUTH_SECRET=change_me_to_another_long_random_string
BETTER_AUTH_URL=http://localhost:3000

# API
PORT=3000
NODE_ENV=development

# Web
NEXT_PUBLIC_API_URL=http://localhost:3000

# Mail (Mailpit for local dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@crm.local

# ─── Channel Adapters ────────────────────────────────────────────────────────
# Email (SendGrid — replace stub with real integration)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
EMAIL_FROM=noreply@yourdomain.com

# SMS (Twilio — replace stub with real integration)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1xxxxxxxxxx
